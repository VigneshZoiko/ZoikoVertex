# X Exhibit screenshots

Drop real product captures of the X publishing workflow here, then wire them into
`src/app/x/page.tsx` (the "See it in action" section) if you want real images instead
of the labeled placeholders.

Recommended captures (PNG, ~1200px wide):
1. connect-x.png   — the OAuth 2.0 X authorization screen / connected @handle state
2. compose.png     — the post composer with AI caption + hashtags and attached media
3. publish-x.png   — the live X preview with the "Publish to X" action

A 30–60s screen recording of connect → compose → preview → publish to X is the single
strongest piece of evidence for the X Developer Exhibit review.
